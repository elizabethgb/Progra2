package adtL;

public interface StackADT {
	void initialize( );
	void push(int x); //initialized stack
	void pop( ); //initialized and not empty stack
	int peek( ); //initialized and not empty stack
	boolean isEmpty( ); //initialized stack
}
